export const placeOrder = async (_orderData: any) => {
  // Mock empty response
  return {};
};

export const fetchOrderStatus = async (_orderId: string) => {
  // Mock empty response
  return {};
};
